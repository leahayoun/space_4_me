<%# onboarding charte de confidentialité %>

<div class="onboarding-container">

  <h1>Bienvenue <%= current_user.first_name %> 👋</h1>

  <p class="description">
    Avant de commencer : voici un petit formulaire pour paramétrer ton profil ^^
  </p>

  <div class="actions">
    <%= link_to "Commencer", next_wizard_path, class: "btn home-btn-primary" %>
  </div>

  <div class="logo">
    <%= image_tag "logo_temp.png", alt: "Space for Me", class: "logo-img" %>
  </div>
</div>

<style>

body {
  font-family: 'DM Sans', system-ui, sans-serif;
}

.onboarding-container {
  max-width: 600px;
  width: 100%;
  padding: 40px;
  background: #fff9f6;
  border: 1px solid #E6CFC5;
  border-radius: 24px;
  box-shadow: 0 12px 32px rgba(0, 0, 0, 0.08);
  text-align: center;
  margin: 0 auto;
}

.description {
  font-size: 1.1rem;
  margin: 20px 0;
  color: #4A3A34;
}

.logo {
  margin-top: 32px;
  display: flex;
  justify-content: center;
}

.logo-img {
  width: 120px;      /* ajuste ici si besoin */
  height: auto;
  opacity: 0.85;
}

</style>
